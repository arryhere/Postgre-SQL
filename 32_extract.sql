SELECT NOW();


SELECT EXTRACT(
    YEAR
    FROM NOW()
  );


SELECT EXTRACT(
    MONTH
    FROM NOW()
  );


SELECT EXTRACT(
    DAY
    FROM NOW()
  );


SELECT EXTRACT(
    HOUR
    FROM NOW()
  );


SELECT EXTRACT(
    MIN
    FROM NOW()
  );


SELECT EXTRACT(
    SECOND
    FROM NOW()
  );


SELECT EXTRACT(
    MILLISECOND
    FROM NOW()
  );


SELECT EXTRACT(
    TIMEZONE
    FROM NOW()
  );